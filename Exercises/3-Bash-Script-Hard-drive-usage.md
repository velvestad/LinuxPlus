**Make a bash script that saves the information of:**
- how much harddrive space is used in procentage
- shows the partition sizes and names
- then the script must run a command that shows how much diskspace the different users on the system has in their homefolders.

The result must be saved to a file called `diskusage.txt`

Append the script you made to the diskusage.txt and send this to the instructor by using Moodle for evaluation.
**(Both the script and the result should be uploaded in the same file)**