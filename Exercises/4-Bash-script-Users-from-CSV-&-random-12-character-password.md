## Create users from a CSV file and give them a random 12 character password.

Copy the list of users here below and save that to a file called `users.csv`

Create a bash script that reads this file and creates the user, the group and then gives the user a random 12 character password.
Create a file with a list of the user names and the assigned passwords, call the file `username.password`

Here below you find the list of user and group names , copy these to the  file `users.csv`
(The list contains only user name and group name.)
```
kalle,kalle
olof,olof
ville,ville
```
Exercise is done when your script is able to create the users from the csv file, give them the random 12 character password and also the script should be able to save the user name and password in a file.


When you have a working script, please upload the script and the file called username.password here so the instructor can look at them