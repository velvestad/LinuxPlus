## Use bash commands to find the specified info needed
Use bash commands **on one line** to get the info that is asked for.

I want you to from the file `/etc/services` find every line that starts with ldap, then I want you to sort the results in reverse into a file called `foundit.txt`, cat this file and take a screenshot showing the command that created this file and the output when you cat the file.

Upload this screenshot to Moodle with the upload function below.